apiVersion: extensions/v1beta1 
kind: Ingress 
metadata:
  name: sockshop-rule
  namespace: sock-shop 
spec:
  rules:
  - host: *
    http:
      paths:
      - path: /
        backend:
          serviceName: front-end
          servicePort: 80
